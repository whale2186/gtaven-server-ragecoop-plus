# RageCoop.Server (Linux Port)

This is a Linux port of `RageCoop Plus Server`, originally built for Windows.


## Installation

1. Download or copy the release archive to your server.
2. Extract the archive:

```bash
tar -xf RageCoop.Server.tar.gz
```

3. Make the server executable:

```bash
sudo chmod +x RageCoop.Server
```

4. Start the server:

```bash
./RageCoop.Server
```

## Notes

* If your archive has a different name, replace `RageCoop.Server.tar.gz` with the correct filename.
* If the file is already executable, you can skip the `chmod` step.
* Run the server from the directory where `RageCoop.Server` was extracted.


## Original Windows Version link
Link: [https://www.gta5-mods.com/scripts/ragecoop](https://www.gta5-mods.com/scripts/ragecoop)

